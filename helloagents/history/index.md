# 历史记录索引

## 2026-06

- `202606061358_不可执行-调研参考_budgeted-adaptive-indexing-research`：归档未执行调研参考方案；作为 budgeted adaptive indexing / 分层 watcher 参数探索的研究材料保留，不作为当前实施入口。后续实际执行应以新的可执行方案包为准。
- `202606011730_部分可执行_startup-cpu-fast-scan-governance`：归档部分可执行方案；启动期 fast scan 背压的关键止血项已在后续 M3/hotset 收缩相关工作中落地或并入 `202606011812_lease-hotset-fast-scan-shrink`，剩余启动期专用预算字段保留为按实测再评估事项。
- `202606011812_lease-hotset-fast-scan-shrink`：完成 M3 lease hotset 正式化与 fast scan 收缩；fast scan 5 秒 SLA 只覆盖 active lease hotset，lease 来源包括查询命中、stale hit、project marker、L0 事件、proc sampler 与 `hot_dirs` 显式配置；普通冷目录由 DirtyQueue、PeriodicColdScan、query repair 和 cold sweep 保持有界最终一致。新增 hotset-only sentinel registry、TTL/续租/驱逐/预算配置、初始 backfill 与真实变化队列拆分，以及 `/watch-state`、`/health`、diagnostics、metrics JSONL 的 hotset/backfill/预算观测字段；`p1_fast_scan_sla` 已改为“hotset 内 5 秒 + 冷目录有界最终一致”。`cargo fmt --check`、`cargo check`、`cargo test -q fast_scan --lib`、`cargo test -q query_hotset_dirs`、`cargo test -q tiered_watch --lib`、`cargo test -q health`、`cargo test -q --test p1_fast_scan_sla`、`git diff --check` 通过。
- `202606130036_proc-sampler-ephemeral-watch`：完成 M2 proc sampler 提前体验件；新增 Linux `/proc/<pid>/fdinfo` 与 `fd` symlink 写句柄采样，只处理同用户进程并受 pid/fd/目录数预算约束；采样目录只作为新鲜度线索送入现有 Ephemeral Watch lease，不作为查询正确性来源。`/watch-state`、`/health`、diagnostics 与 metrics JSONL 新增 `proc_sampler_*` 字段，暴露采样耗时、pids seen/scanned/denied、fdinfo/readlink/write fd、sampled dirs、triggered watches、budget exhausted 与 unavailable。`cargo test -q proc_sampler`、`cargo test -q health`、`cargo test -q tiered_watch`、完整 `cargo test -q`、`cargo fmt --check`、`git diff --check` 通过。
- `202606122324_sliced-repair-health-observability`：完成 M1-3 分片 repair 与有界最终一致观测；`DirtyQueueEntry` 增加 runtime-only `DirtyRepairCursor`，Periodic cold scan 对大目录按约 512 entries / 20ms 分片执行，未完成 slice 携带 cursor 重入 DirtyQueue，StartupRepairDeferred 保留递归 fast-sync 以覆盖启动期 rename subtree；`/health`、diagnostics 与 metrics JSONL 新增 `cold_sweep_last_completed`、`cold_sweep_period_estimate`、`dirty_backlog`，用于解释“返回结果是真的，但候选集合可能暂时不全”的追平上界。`cargo test -q sliced_repair`、`cargo test -q health`、`cargo test -q query`、完整 `cargo test -q`、`cargo fmt --check`、`git diff --check` 通过。
- `202606122300_runtime-subtree-tombstone`：完成 M1-2 运行时 subtree tombstone；Delete 父目录和 RenameFrom 创建 `RuntimeSubtreeTombstone { root_path, generation, expires_at }`，cold/base 查询候选进入 `QueryVerifyBudget` 前按 prefix filter 屏蔽旧子路径，不执行同步 `metadata()`；TTL 清理、同名目录重建解除过滤、runtime-only 不持久化边界均已覆盖。`cargo test -q subtree_tombstone`、`cargo test -q query_verify`、`cargo test -q query`、完整 `cargo test -q` 通过。
- `202606122238_topk-query-verify`：完成 M1-1 查询返回前 Top-K 验真；新增 `[query] max_verify_per_query=150`、`verify_timeout_ms=75`、`allow_sync_readdir=false` 配置并接入 `TieredIndex`，冷层/base 查询返回前使用 `QueryVerifyBudget` 约束同步 `stat`；`lazy_validation_enabled` 默认关闭，仅作为显式低功耗后台补偿路径。删除路径不返回，mtime/identity 变化返回 `Changed` 且 `validated=true`，宽泛 stale 查询同步验证数不超过预算；`cargo test -q query_verify`、`cargo test -q lazy_validation`、`cargo test -q query`、完整 `cargo test -q` 通过。
- `202606061413_fix-rename-event-missing-new-file`：完成 M0 rename 可见性修复；`src/event/stream.rs` merge 阶段只把孤立 `RenameMode::To` 当 `Create`，孤立 `From` / 单路径 `Any` 保持普通修改语义；同批次 `Create` / `Rename` 不再被后续 `Modify` 覆盖。新增 stream 单元测试与 `p1_visibility_latency` 下载场景验证，`cargo test -q rename`、`cargo test -q merge`、`cargo test -q stream`、`cargo test -q download_rename`、完整 `cargo test -q` 全通过。
- `202606011821_ci-log-regression-closure`：排查 `/home/shiyi/Downloads/vcp-FD/logs` 中 CI 失败；确认 `fd-rdd Stress Tests` 仅 `test-hardlink-crossmount` 实际失败，其余脚本 FAIL 文本多为分支打印；`CI` workflow 多节点失败均为同一 `fast_sync_reconciles_add_and_delete` 断言。已将 fast-sync 测试对齐 Linux inode 复用下 single same-FileKey upsert 的最终一致语义，并将 stress CI hardlink 检查更新为 PathEntry 多别名策略；`cargo fmt --check`、相关定向测试和完整 `cargo test -q` 通过。
- `202606011707_fast-scan-sla-regression`：修复 L1/L2 fast scan SLA 回归；`l1_l2_fast_scan_bootstrap_budget_per_tick` 恢复为每 tick 注册预算，不再因已注册 sentinel 达到单批预算而停止覆盖后续已知目录；新注册 sentinel 会进入一次 `FastScanChangedDir` 初始补扫，补齐覆盖前已经发生的文件创建；同步 README、CHANGELOG 与 watcher wiki，`cargo test -q fast_scan` 和完整 `cargo test -q` 通过。
- `202606171100_code-debt-cleanup-phase1`：完成代码债务清理 Phase 1 全部 4 项任务。P0：`HardlinkDupe`/`ContentDupe` 桩代码改为返回 `QueryCompileError::UnsupportedAtom`，不再静默返回 True。P1：`unix_secs()` 提取到 `util.rs`，6 处本地副本删除。P1：`l2_partition` 段导出去重，常量提升为模块级 `const`，抽取 `build_v6_segments()` 和 `write_v6_segments_to_writer()` 公共函数。P2：`io_governor.rs` 的 `std::sync::Mutex` 替换为 `parking_lot::Mutex`，移除所有 `.lock().unwrap()`。完整 `cargo test -q` 通过。
- `202606191500_code-review-fixes-phase1`：完成代码审查修复第二轮 3 项任务。修复 `storage/snapshot_v7.rs` HashMap 非确定性（遍历前按 FileKey 排序）；修复 `index/l2_partition/tests.rs` 恒真断言（删除无意义 assert）；修复 `query/dsl.rs` 2 处 `unwrap()` 改为 `expect()` 带诊断信息。
- `202606191800_fix-snapshot-v7-redundant-sort`：修复 `snapshot_now_v7` 冗余双重排序；删除 `snapshot_v7.rs` 原第 1691-1693 行的冗余 `sort_by_key()` 调用及注释，`cargo check` 编译通过。
- `202606200645_scan-stat-storm-optimization`：完成周期扫描 stat() 风暴优化，消除 1M 文件规模下每扫描周期 300K 次 `symlink_metadata()` 系统调用的瓶颈。Phase 1（dir mtime 预检）：`DirectoryManifest` 新增 `dir_mtime_ns` 字段及 `dir_unchanged`/`record_dir_mtime` 方法，`scan_dir_repair_slice_with_project_markers` 在 manifest skip 之后、sliced scan 之前插入目录 mtime 比对——若目录 mtime 未变则跳过整个子树扫描（300K stat→1 stat）。Phase 2（readdir 批量删除对齐）：delete alignment 循环从逐文件 `symlink_metadata` 改为 `read_dir` + HashSet 差集比对，一次 readdir 替代 N 次 stat。Phase 3（manifest 上限放宽）：`directory_manifest_summary_bounded` 的 512 entry 截断放宽，大目录不再跳过 manifest 检查。涉及文件：`src/index/tiered/directory_manifest.rs`（+288）、`src/index/tiered/sync.rs`（+367/-88）、`src/index/tiered/lazy_validation.rs`（+8）、`src/index/tiered/tests.rs`（+844）。commit `6d7eeb8`，clippy 修复 `fd37829`。

## 2026-05

- `202605311828_l1-l2-fast-scan-sla`：落地 L1/L2 fast scan lane MVP；本地可信文件系统上的已知非 L0 目录默认按 5 秒目标执行 sentinel tick，网络/FUSE/未知 mount 默认 best-effort 且不报告 strict SLA；新增 fast scan 配置、mount 分类、`DirSentinel` runtime state、`DirtyReason::FastScanChangedDir`、统一 dirty apply 接入，以及 `/watch-state`、`/health`、diagnostics 和 metrics JSONL 观测字段；根 README 的 `fd-rdd-sim` 长手册迁移到 `src/sim/README.md`，并补充 fast scan runtime-only 映射说明；完整 `cargo test -q` 与 `cargo test -q --features mimalloc` 通过。
- `202605311438_startup-soft-repair-deferred`：完成启动 soft repair 延后与精准脏化；`unclean_shutdown` / WAL tail damage 不再驱动前台 startup repair，新增 `startup_scan_required`、deferred repair 状态与 `/health`/diagnostics/metrics 字段；WAL replay 改为 valid prefix + `WalReplayDamage`，坏 frame 定位父目录或回退 unknown scope；新增低优先级 `StartupRepairDeferred`、lazy validation 后台限流校验、低优先级扫描全局版本防覆盖，以及 v7 recovery audit 浅审计，完整 `cargo test -q` 通过。
- `202605301506_recovery-evidence-and-memory-overhead`：收紧启动恢复证据分层，将 `unclean_shutdown` / WAL tail truncated 作为 soft repair evidence，只触发补扫；`bad_manifest`、`missing_segment`、`wal_gap`、`bad_current_wal` 等 hard evidence 保留 rebuild fallback；`startup_repair_budget_ms` 接入启动补扫预算，`force_rebuild_ratio` 保留为扫描后的次级升级条件；`/memory` 默认改为轻量快照并支持 `?full=true` 完整采样，metrics 与 memory report loop 复用缓存；运行态复核进一步修复 WAL v4 current WAL 被误判为 `bad_current_wal` 的 rebuild 触发点、snapshot 后 light memory cache 未失效导致 L2 统计过期的问题、启动 fast-sync 使用错误 cutoff 并重复 upsert 未变化 base path 导致 L2/overlay/RSS 高峰的问题，以及 strict watcher 启动时整棵 L0 注册超大 hot dir 导致 7 万级 inotify watch 的问题。
- `202605301328_version-7-release-metadata`：将项目发布基线升级为 v7.0.0，同步 Cargo/Cargo.lock、README 多语言版本标识、CHANGELOG 7.0.0 发布段、AUR `PKGBUILD`/`.SRCINFO`、测试 README、编年史和架构评审报告；AUR `fd-rdd-git` 保持 `7.0.0` 静态基线，`pkgver()` 构建时生成 `7.0.0.r<count>.<hash>`。
- `202605301207_review-closure-dupe-content-smoke`：补齐 review 遗漏，`dupe:content` 复用内容读取准入策略并在 query generation guard 外执行 partial/full hash；新增 exclude/oversized/mount policy 回归测试；`scripts/smoke-search-syntax.sh` 支持临时 root、临时端口和自启动 daemon；同步 README/CHANGELOG/wiki，并明确 `memory_light` 长稳 RSS/P95 对照仍为外部环境验收项。
- `202605291305_runtime-boundary-closure`：完成批次 1 Runtime Boundary Closure，收口 watcher/scan mount policy、FUSE probe timeout cache、quarantine sidecar verify、Freeze Gate 恢复顺序、case policy runtime diagnostics、clock skew 对账和 I/O Governor diagnostics；补齐普通 ext4/xfs、关闭 I/O Governor、正常时间流逝 mtime 剪枝、HTTP 默认禁用、roots 配置兼容、拒绝挂载点 fixture 和 full build governor 计数回归。
- `202605291314_arcswap-zero-copy-governance`：完成批次 2A/zero-copy governance 对账与补齐，确认 HTTP/UDS/internal query guard 边界、generation strong refs、query guard 指标、rebuild cooldown/coalesce、MemoryReport pending/dirty/guard/generation 字段、cold v7 mmap no-hydration、alignment 单测和默认关闭的 mmap warmup；冷启动 latency 数值对照保留为非沙箱 daemon 环境外部采集项。
- `202605291311_query-experience-features`：完成批次 4/5 Query Experience Features，落地目录 entry 与 `type:dir` / `type:file`、`empty:`、hardlink `dupe:`、`dupe:content`、默认关闭的 `content:` / `text:` 内容索引查询、HTTP result type/reason/confidence 与 diagnostics hash/content 指标；2026-05-30 review 进一步补齐 `dupe:content` 的策略准入与 guard 外 hash 回归。
- `202605291313_disk-first-memory-light`：完成批次 2B memory_light profile，支持 CLI/config 选择低内存运行时配置，接入更积极 overlay/周期 flush、WAL seal/checkpoint 边界和 rebuild cooldown 策略；已完成配置、flush、WAL、full-scan metrics、README/CHANGELOG 与小规模 smoke，长稳 RSS/P95 对照保留为外部环境跟踪项。
- `202605291312_watcher-balanced-evolution`：完成批次 6 Watcher Balanced Evolution，落地 project marker 识别与 project root candidate、L0 promotion / Ephemeral Watch lease / high-priority L1 fallback、nested project debug 解释、Directory Manifest Crawler、exclude-aware watch cost 与 fanotify 预研；补齐 acceptance 自动化验收并通过完整 `cargo test -q`。
- `202605291315_storage-legacy-cleanup`：完成批次 7 Storage Legacy Cleanup，运行时 `StorageBackend` 不再暴露 legacy v2-v6 snapshot / old LSM read API；`refresh_base()` 收敛为 compatibility/test boundary 并在 `/health.diagnostics.storage` 与 metrics 暴露调用计数；常规 query / fast-sync 改为只读 L2 warm-memory 兜底和 stale 校验，不再隐式全量 materialize；补齐 L1 rename/delete/recreate、旧快照兼容、WAL replay、snapshot/rebuild 与 cold mmap 回归。
- `202605291125_runtime-boundary-state-contract`：落地 Runtime Boundary State Contract 第一阶段，WAL 增加 `OFFLINE_ROOT`/`ONLINE_ROOT` root 状态记录，新增 quarantine sidecar、Freeze Gate、强类型 `/health.diagnostics`、结构化 `[[roots]]` 配置、case policy 探测基础、clock skew dirty window 与 root/system HTTP 默认禁用策略；I/O governor 真实 loop metrics、pathconf 探测和 sidecar 后台验证线程保留为后续增强。
- `202605281133_path-entry-hardlinks`：执行 PathEntry 硬链接别名修复，移除扫描阶段 inode 去重，查询/materialize 改为 path/docid 去重，按路径删除不再影响同 inode 其他别名，并补硬链接 full build、delete、rename、v7 snapshot reload 回归测试。
- `202605281134_mount-offline-quarantine`：落地 mountinfo 解析、默认远程/虚拟 fstype policy 与 delete circuit breaker 基础能力；quarantine sidecar、offline health 和恢复复活流程延期到持久化状态批次。
- `202605281135_case-policy-folding`：新增 `CasePolicy` 与 Unicode lookup fold helper，trigram 候选按 fold 后 byte window 生成，补 `ß -> ss` 长度变化回归；per-root 自动探测与 conflict diagnostics 延期。
- `202605281136_io-governor`：新增 Linux `ioprio_set(IOPRIO_CLASS_IDLE)` best-effort 封装、token bucket、PSI parser 与 backoff；full build/rebuild/fast-sync 线程启动时尝试 idle I/O priority，EPERM 等失败优雅降级。
- `202605281137_clock-skew-guard`：新增 clock skew detector、mtime cutoff trust helper，并将 WAL seal id 改为 `max(now, last+1)` 单调策略；runtime loop 入队与 health/metrics 延期。
- `202605281138_fs-boundary-policy`：新增 mount table / fstype policy 模块，full build、fast-sync、scan_dirs/startup repair walker 接入默认 mount policy，并新增后台线程超时探测 helper，避免 FUSE 死挂阻塞扫描线程。
- `202605281139_multi-user-query-boundary`：新增运行身份、HTTP policy 和 overfetch 权限过滤 helper，HTTP `/scan` 限制在 configured roots 内；access-check worker 与安全审计 metrics 延期。
- `202605271158_recovery-ledger-hardening`：新增恢复账本只读审计，启动期审计 stable/prev/legacy v7、LSM manifest/segment/sidecar、orphan/tmp 与 WAL gap；`load_or_empty*` 改为按可信 checkpoint 回放 WAL，补齐 WAL replay 摘要、durability 配置与 `/health` 暴露，并增加恢复审计、checkpoint、删除/rename repair 和 durability 专项测试。
- `202605221210_strict-watch-budget`：新增 tiered watcher strict/balanced/low_power profile；strict 模式默认 65536 watch 预算并要求 required hot dirs 全部进入 L0，`/watch-state` 与 `/health` 输出 coverage cost、shortfall、uncovered dirs 和 fail-hard 严重级别；同时修正 L3 为 `ScannedFresh` / `EventuallyConsistent` 语义并完成本机 strict 验证。
- `202605202150_watch-rss-attribution`：定位 125MB RSS 的主要来源为 `/run/user/1000/fd-rdd/index.d/stable.v7` tmpfs mmap 校验页，并发现 tiered watch 估算按排除后目录数低估实际 inotify wd；修复 v7 mmap 校验后 RSS 回吐、notify 真实递归 watch 成本估算、单根超预算拒绝进入 L0，以及 `/health` 严重级别。
- `202605202134_snapshot-cold-remount`：修复 snapshot 后当前进程把 manifest-only cold base 重新热化的问题；snapshot 写出 v7 后立即 cold remount，避免 base 路径表回到 50 万 hot entries。
- `202605202111_observability-diagnostics`：扩展 metrics JSONL 为统一诊断快照，保留 `/watch-state` 顶层字段兼容旧查询，并新增 runtime/memory/health/diagnostics 嵌套对象；`/health` 拆分底层 event watcher 降级与 tiered 非 L0 冷层目录口径，`/memory` 增加 swap 字段。
- `202605101955_remove-size-contract`：移除公开 `size` 查询契约，`size:` 与 `sort=size` 现在显式返回错误；`/search` 响应删除 `size` 字段并记录 breaking change；v7 单文件快照升级到 version 2，同时兼容旧 version 1 的 40B entry。
- `202605101902_review-fixes`：补齐 review 遗漏，v7 snapshot 写入阶段生成完整路径 trigram posting 并持久化 `[0,0,0]` sentinel，使 manifest-only cold segment 可安全使用 mmap postings；`fd-rdd-sim` grid/evolve/optimize 搜索空间主动枚举和变异 interval/validate_on_query/disabled 三种 L3 策略模式。
- `202605101648_l3-policy-cold-mmap`：补齐 L3 scan policy 配置/调度/sim 回填语义，并将 manifest-only cold segment 查询、metadata lookup、parent candidates 改为直接按需读取 v7 mmap 段；冷段 trigram 预过滤无法证明候选完整时回退全段精确过滤，避免冷查询全量 hydration 与目录组件漏查。
- `20260510_ephemeral-watch-p8`：完成 P8 Ephemeral Watch，新增独立预算的临时 watcher 租约、TTL/idle/无变化/L0 覆盖/预算驱逐销毁条件、watcher command 接入、观测字段和租约测试。
- `202605092249_l3-scan-policy`：完成 P3 L3 语义明确化，新增 interval/validate_on_query/disabled 策略和独立 L3 扫描周期，runtime 与 sim 对齐并补回归测试。
- `202605092202_observability-p0`：补齐 P0 观测闭环，扩展 `/debug/tiered-watch` 单目录字段，接实 `/watch-state` dirty/cold/stale 计数，并新增 runtime/sim report 字段映射文档。
- `202605092139_sim-runtime-parity-config`：补齐 `fd-rdd-sim` 与 runtime parity 观测指标，新增替换/降级形状测试，并支持从 benchmark report 导出 tiered watcher TOML patch。
- `202605061850_fd-rdd-sim`：新增 `fd-rdd-sim` synthetic 压测框架，支持 optimize 持续试错收敛、single/grid/evolve/adversarial 辅助模式，评估 tiered watcher SLA、延迟与资源预算并输出推荐配置。
- `202605052009_tiered-watch-hotness-scheduler`：tiered watcher 增加 L1/L2/L3 热度调度、next scan、冷 L0 替换和 `/watch-state` 扩展观测。
- `202605032133_tiered-dynamic-watch-budget`：动态新目录 watch 注册接入 tiered runtime 预算，修复 Create(Folder) 绕过 `max_watch_dirs` 的边界。
- `202605032133_metrics-runtime`：`/metrics` 接入真实 `StatsCollector`，覆盖查询、事件、snapshot 与 fast-sync 计数。
- `202605032133_recovery-long-run-tests`：新增 SIGTERM final snapshot 重启恢复测试，并加入 poweroff recovery CI 专项。
- `202605032133_fallback-degraded-observability`：补强 `/watch-state`、`/health` 与 stress CI 对 fallback/degraded 场景的诊断与等待逻辑。
- `202605031957_tiered-watch-runtime`：将 tiered watcher preview 升级为 L0/L1 动态闭环，补充 watcher control channel、ScanOutcome/freshness 检测和真实 `/watch-state` runtime 聚合。
- `202605021500_v7-direct-base-load`：v7 快照直接挂载为 BaseIndexData，避免启动回灌 L2；snapshot 边界改写当前可见全集并补齐 v7 FileEntryIndex 反序列化排序重建。
- `202605021430_large-scale-pressure-tuning`：跑通 ignored 大规模压测，修复新目录 watch 竞态，冷启动改保守串行并节流，压低 initial_indexing CPU/RSS 峰值。
- `202605021313_hotpath-materialize-boundary`：将 BaseIndex/ParentIndex 全量 materialize 从普通事件热路径移到 snapshot/rebuild/兼容边界，fast_sync 删除对齐改走 BaseIndex。
- `202605021152_persistentindex-fileentry-paths`：将 `PersistentIndex` 运行时主存储迁移到 `FileEntry + paths`，旧 `CompactMeta + PathArena` 降级为兼容读写格式。
- `20260502_wrapup-stabilization`：收敛 causal-chain 与 wrap-up 后续合并问题，恢复编译/测试，移除 `disk_layers` 热路径和目录 rename 同步深扫。
